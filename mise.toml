# SPDX-License-Identifier: MPL-2.0
# Panoply toolchain pins (mise). Packager remains Guix (`build/guix.scm`).
# JS reach: Bun → Deno → pnpm → npm.

[tools]
just = "1.40.0"
zig = "0.15.1"
bun = "1.2.21"
deno = "2.5.0"

[env]
# Idris2 is not in the mise registry; install via Guix or the Idris2 installer.
