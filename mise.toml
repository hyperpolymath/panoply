# SPDX-License-Identifier: MPL-2.0
# Panoply toolchain pins (mise).
# JS reach order (owner 2026-09-20): Bun → Deno → pnpm → npm.
# Packager remains Guix (`build/guix.scm`); mise is a local version helper.

[tools]
just = "latest"
zig = "0.15.1"
bun = "latest"
deno = "latest"

[env]
# Idris2 is not a mise registry tool; install via Guix.
